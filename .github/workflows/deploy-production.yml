name: 🟢🟢 Promote Staging to Production (on PR merge to main)

on:
  pull_request:
    branches: [ main ]
    types: [ closed ]  # dispara quando PR é fechado (merge ou apenas fechado)

permissions:
  contents: write

jobs:
  promote:
    if: github.event.pull_request.merged == true  # garante que foi MERGE
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Promover staging → produção (copiar index)
        run: |
          test -f staging/index.html || (echo "staging/index.html não encontrado no gh-pages" && exit 1)
          cp -f staging/index.html index.html
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "promote: staging → production (via PR merge na main)" || echo "Sem mudanças para promover"
          git push origin gh-pages